# Database Configuration
DATABASE_URL=postgresql://siebert:siebert_dev_password@postgres:5432/siebert

# Environment
NODE_ENV=production

# Authentication
JWT_SECRET=siebert-services-secret-key-2024

# Server Port (optional, defaults to 8080)
PORT=8080

# Optional: Email/SMTP Configuration (for live chat, contact form)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password
# SMTP_FROM=noreply@siebertservices.com

# Optional: Logging
# LOG_LEVEL=info
