# Git
.git
.gitignore

# Dev environment
.replit
.upm
.env
.env.local
.env.*.local
.vscode
.idea
.DS_Store

# Logs
*.log
npm-debug.log*

# Temp files
*.swp
*.swo
*~
.cache
coverage
.turbo

# Exclude mockup-sandbox (dev-only, not needed in Docker)
artifacts/mockup-sandbox

# Never exclude dist folders - we need them if pre-built
# Never exclude node_modules exclusion to keep image small
# (Docker rebuilds them from lockfile)
